#include <bits/stdc++.h>
#include <iostream>
using namespace std;


int pickingNumbers(vector<int> a) {
    
    for (int x=0; x<a.size(); x++) {
        if(){

        }
    }
}


int main(int argc, char **argv){
    ios_base::sync_with_stdio(false); //fast I/O
    cin.tie(NULL);                    //
                                      //
                                      //
    cout<<pickingNumbers(vector<int> {1,2,2,3,1,2});

     //read n times from std input 
   /*
    vector<int> vec;
    int num;
    cin>> num; 
    while (num!=0){
        int aux; 
        cin>>aux; 
        vec.push_back(aux);
    }
    */
    return 0;
}
